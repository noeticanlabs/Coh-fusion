namespace CohFusion.Numeric

/--
Types permitted in deterministic consensus/runtime evaluation.
These types are allowed in the verifier kernel and must not depend on IEEE-754 Float.
-/
class ConsensusSafe (α : Type) : Prop where

end CohFusion.Numeric
